name: 🚀 Deploy Node.js Project to VM

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      project_url:
        description: 'GitHub URL of Node.js project to deploy'
        required: false
        default: ''

env:
  # GitHub URL of the Node.js project to deploy
  NODE_PROJECT_URL_TO_DEPLOY: ${{ secrets.NODE_PROJECT_URL_TO_DEPLOY || github.event.inputs.project_url }}
  
  # App configuration
  APP_NAME: nodejs-app
  APP_DIR: /home/${{ secrets.SSH_USER }}/apps/nodejs-app
  APP_PORT: 3000
  NODE_VERSION: 20

jobs:
  deploy:
    name: Setup & Deploy Node.js Application
    runs-on: ubuntu-latest
    
    steps:
      # ==========================================
      # Step 1: Validate Environment Variables
      # ==========================================
      - name: ✅ Validate Configuration
        run: |
          echo "Validating required environment variables..."
          
          if [ -z "${{ env.NODE_PROJECT_URL_TO_DEPLOY }}" ]; then
            echo "❌ ERROR: NODE_PROJECT_URL_TO_DEPLOY is not set!"
            echo "Please set it in GitHub Secrets or provide via workflow_dispatch"
            exit 1
          fi
          
          if [ -z "${{ secrets.SSH_HOST }}" ]; then
            echo "❌ ERROR: SSH_HOST secret is not set!"
            exit 1
          fi
          
          if [ -z "${{ secrets.SSH_USER }}" ]; then
            echo "❌ ERROR: SSH_USER secret is not set!"
            exit 1
          fi
          
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "❌ ERROR: SSH_PRIVATE_KEY secret is not set!"
            exit 1
          fi
          
          echo "✅ All required variables are set!"
          echo "📦 Project to deploy: ${{ env.NODE_PROJECT_URL_TO_DEPLOY }}"
          echo "🖥️  Target host: ${{ secrets.SSH_HOST }}"

      # ==========================================
      # Step 2: Initial VM Setup (One-time)
      # ==========================================
      - name: 🛠️ Setup VM Environment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          command_timeout: 30m
          script: |
            set -e
            
            echo "================================"
            echo "🚀 Starting VM Setup"
            echo "================================"
            
            # Update system packages
            echo "📦 Updating system packages..."
            sudo apt-get update
            
            # Install essential packages
            echo "📦 Installing essential packages..."
            sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
              curl \
              wget \
              git \
              build-essential \
              ufw \
              --no-install-recommends
            
            # Install Node.js
            if ! command -v node &> /dev/null; then
              echo "📦 Installing Node.js ${{ env.NODE_VERSION }}..."
              curl -fsSL https://deb.nodesource.com/setup_${{ env.NODE_VERSION }}.x | sudo -E bash -
              sudo apt-get install -y nodejs
            else
              echo "✅ Node.js already installed: $(node -v)"
            fi
            
            # Install PM2
            if ! command -v pm2 &> /dev/null; then
              echo "📦 Installing PM2..."
              sudo npm install -g pm2
            else
              echo "✅ PM2 already installed: $(pm2 -v)"
            fi
            
            # Install Nginx
            if ! command -v nginx &> /dev/null; then
              echo "📦 Installing Nginx..."
              sudo apt-get install -y nginx
            else
              echo "✅ Nginx already installed"
            fi
            
            # Configure UFW (Firewall)
            echo "🔥 Configuring firewall..."
            sudo ufw --force enable
            sudo ufw allow OpenSSH
            sudo ufw allow 'Nginx Full'
            sudo ufw allow 80/tcp
            sudo ufw allow 443/tcp
            sudo ufw status
            
            # Create application directory
            echo "📁 Creating application directory..."
            mkdir -p ${{ env.APP_DIR }}
            
            # Setup PM2 startup script
            echo "🔄 Configuring PM2 startup..."
            pm2 startup systemd -u ${{ secrets.SSH_USER }} --hp /home/${{ secrets.SSH_USER }} | grep "sudo" | sudo bash || true
            
            echo "✅ VM Setup Complete!"
            echo "================================"

      # ==========================================
      # Step 3: Clone & Setup Application
      # ==========================================
      - name: 📥 Clone and Setup Application
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          command_timeout: 30m
          script: |
            set -e
            
            echo "================================"
            echo "📥 Cloning Application"
            echo "================================"
            
            cd ${{ env.APP_DIR }}
            
            # Remove existing files if present
            if [ -d ".git" ]; then
              echo "🗑️  Removing existing repository..."
              cd ..
              rm -rf ${{ env.APP_DIR }}
              mkdir -p ${{ env.APP_DIR }}
              cd ${{ env.APP_DIR }}
            fi
            
            # Clone the repository
            echo "📥 Cloning from: ${{ env.NODE_PROJECT_URL_TO_DEPLOY }}"
            git clone ${{ env.NODE_PROJECT_URL_TO_DEPLOY }} .
            
            # Detect package manager and install dependencies
            echo "📦 Installing dependencies..."
            if [ -f "package-lock.json" ]; then
              echo "Using npm..."
              npm ci --production
            elif [ -f "yarn.lock" ]; then
              echo "Using yarn..."
              npm install -g yarn
              yarn install --production
            elif [ -f "pnpm-lock.yaml" ]; then
              echo "Using pnpm..."
              npm install -g pnpm
              pnpm install --prod
            else
              echo "Using npm install..."
              npm install --production
            fi
            
            # Run build if build script exists
            if grep -q '"build"' package.json 2>/dev/null; then
              echo "🔨 Running build..."
              npm run build || echo "⚠️  Build script failed or not needed"
            fi
            
            echo "✅ Application setup complete!"
            echo "================================"

      # ==========================================
      # Step 4: Detect Entry Point & Start Script
      # ==========================================
      - name: 🔍 Configure Application
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            set -e
            cd ${{ env.APP_DIR }}
            
            echo "🔍 Auto-detecting application configuration..."
            
            # Create logs directory
            mkdir -p logs
            
            # Method 1: Use npm start if available
            if grep -q '"start"' package.json 2>/dev/null; then
              echo "✅ Using npm start"
              USE_NPM_START=true
            else
              USE_NPM_START=false
              
              # Method 2: Find entry point
              echo "🔍 Detecting entry point file..."
              ENTRY=""
              
              # Check package.json main field
              ENTRY=$(node -pe "try{require('./package.json').main}catch(e){''}" 2>/dev/null || echo "")
              
              # If not found, search common locations
              if [ -z "$ENTRY" ] || [ ! -f "$ENTRY" ]; then
                for f in index.js server.js app.js main.js src/index.js src/server.js src/app.js src/main.js dist/index.js build/index.js; do
                  if [ -f "$f" ]; then
                    ENTRY="$f"
                    break
                  fi
                done
              fi
              
              # Verify file exists
              if [ -z "$ENTRY" ]; then
                echo "❌ ERROR: Could not find entry point!"
                echo "Available files:"
                find . -maxdepth 3 -name "*.js" -type f | head -20
                exit 1
              fi
              
              if [ ! -f "$ENTRY" ]; then
                echo "❌ ERROR: Entry file does not exist: $ENTRY"
                exit 1
              fi
              
              echo "✅ Found entry point: $ENTRY"
            fi
            
            # Create ecosystem.config.js
            if [ "$USE_NPM_START" = true ]; then
              cat > ecosystem.config.js << 'ENDPM2'
              module.exports = {
                apps: [{
                  name: 'nodejs-app',
                  script: 'npm',
                  args: 'start',
                  instances: 1,
                  autorestart: true,
                  watch: false,
                  max_memory_restart: '500M',
                  env: { NODE_ENV: 'production', PORT: 3000 },
                  error_file: './logs/err.log',
                  out_file: './logs/out.log',
                  log_date_format: 'YYYY-MM-DD HH:mm:ss'
                }]
              };
              ENDPM2
            else
              cat > ecosystem.config.js <<ENDPM2
              module.exports = {
                apps: [{
                  name: 'nodejs-app',
                  script: '${ENTRY}',
                  instances: 1,
                  autorestart: true,
                  watch: false,
                  max_memory_restart: '500M',
                  env: { NODE_ENV: 'production', PORT: 3000 },
                  error_file: './logs/err.log',
                  out_file: './logs/out.log',
                  log_date_format: 'YYYY-MM-DD HH:mm:ss'
                }]
              };
              ENDPM2
            fi
            
            echo "✅ Created ecosystem.config.js"
            cat ecosystem.config.js

      # ==========================================
      # Step 5: Configure Nginx Reverse Proxy
      # ==========================================
      - name: 🌐 Configure Nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            set -e
            
            echo "🌐 Configuring Nginx..."
            
            # Create Nginx configuration
            sudo tee /etc/nginx/sites-available/${{ env.APP_NAME }} > /dev/null << 'EOF'
            server {
                listen 80 default_server;
                listen [::]:80 default_server;
                
                server_name _;
                
                # Security headers
                add_header X-Frame-Options "SAMEORIGIN" always;
                add_header X-Content-Type-Options "nosniff" always;
                add_header X-XSS-Protection "1; mode=block" always;
                
                # Logging
                access_log /var/log/nginx/${{ env.APP_NAME }}_access.log;
                error_log /var/log/nginx/${{ env.APP_NAME }}_error.log;
                
                location / {
                    proxy_pass http://localhost:${{ env.APP_PORT }};
                    proxy_http_version 1.1;
                    
                    # WebSocket support
                    proxy_set_header Upgrade $http_upgrade;
                    proxy_set_header Connection 'upgrade';
                    
                    # Proxy headers
                    proxy_set_header Host $host;
                    proxy_set_header X-Real-IP $remote_addr;
                    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                    proxy_set_header X-Forwarded-Proto $scheme;
                    
                    # Timeouts
                    proxy_connect_timeout 60s;
                    proxy_send_timeout 60s;
                    proxy_read_timeout 60s;
                    
                    # Buffer settings
                    proxy_buffering on;
                    proxy_buffer_size 4k;
                    proxy_buffers 8 4k;
                    
                    proxy_cache_bypass $http_upgrade;
                }
                
                # Increase max body size for file uploads
                client_max_body_size 50M;
            }
            EOF
            
            # Enable site and remove default
            sudo ln -sf /etc/nginx/sites-available/${{ env.APP_NAME }} /etc/nginx/sites-enabled/
            sudo rm -f /etc/nginx/sites-enabled/default
            
            # Test Nginx configuration
            echo "🧪 Testing Nginx configuration..."
            sudo nginx -t
            
            # Restart Nginx
            echo "🔄 Restarting Nginx..."
            sudo systemctl restart nginx
            sudo systemctl enable nginx
            
            echo "✅ Nginx configured and running!"

      # ==========================================
      # Step 6: Start Application with PM2
      # ==========================================
      - name: 🚀 Start Application
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            set -e
            
            cd ${{ env.APP_DIR }}
            
            echo "🚀 Starting application with PM2..."
            
            # Stop and delete existing app if running
            pm2 delete ${{ env.APP_NAME }} 2>/dev/null || echo "No existing app to delete"
            
            # Verify ecosystem.config.js exists
            if [ ! -f "ecosystem.config.js" ]; then
              echo "❌ ERROR: ecosystem.config.js not found!"
              exit 1
            fi
            
            echo "📄 Using configuration:"
            cat ecosystem.config.js
            
            # Start application
            echo ""
            echo "▶️  Starting PM2..."
            pm2 start ecosystem.config.js
            
            # Wait a moment for app to start
            sleep 3
            
            # Check if app started successfully
            if pm2 list | grep -q "online"; then
              echo "✅ Application started successfully!"
            else
              echo "❌ Application failed to start!"
              echo "📋 PM2 Status:"
              pm2 status
              echo ""
              echo "📝 PM2 Logs:"
              pm2 logs ${{ env.APP_NAME }} --lines 50 --nostream
              exit 1
            fi
            
            # Save PM2 process list
            pm2 save
            
            # Show status
            echo ""
            echo "📊 Current Status:"
            pm2 status
            
            echo ""
            echo "📝 Recent Logs:"
            pm2 logs ${{ env.APP_NAME }} --lines 20 --nostream

      # ==========================================
      # Step 7: Health Check
      # ==========================================
      - name: 🏥 Health Check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo "🏥 Running health check..."
            echo "⏳ Waiting for application to start..."
            sleep 10
            
            # Try to access the application
            MAX_ATTEMPTS=15
            ATTEMPT=1
            
            while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
              echo "Attempt $ATTEMPT/$MAX_ATTEMPTS..."
              
              # Try localhost first
              if curl -f -s http://localhost:${{ env.APP_PORT }} > /dev/null 2>&1; then
                echo "✅ Application is responding on port ${{ env.APP_PORT }}!"
                break
              fi
              
              if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
                echo "❌ Health check failed after $MAX_ATTEMPTS attempts"
                echo "📝 Checking PM2 logs:"
                pm2 logs ${{ env.APP_NAME }} --lines 20 --nostream
                exit 1
              fi
              
              ATTEMPT=$((ATTEMPT + 1))
              sleep 3
            done
            
            # Test through Nginx
            echo ""
            echo "🌐 Testing Nginx proxy..."
            if curl -f -s http://localhost > /dev/null 2>&1; then
              echo "✅ Nginx proxy is working!"
            else
              echo "⚠️  Nginx proxy may have issues"
            fi

      # ==========================================
      # Step 8: Deployment Summary & Live URL
      # ==========================================
      - name: 📊 Deployment Summary
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            echo ""
            echo "========================================"
            echo "✅ DEPLOYMENT SUCCESSFUL!"
            echo "========================================"
            echo ""
            echo "📦 Application: ${{ env.APP_NAME }}"
            echo "🔗 Repository: ${{ env.NODE_PROJECT_URL_TO_DEPLOY }}"
            echo "📍 Deployed to: ${{ env.APP_DIR }}"
            echo ""
            echo "🌐 LIVE URL (accessible from internet):"
            echo "========================================"
            echo "   http://${{ secrets.SSH_HOST }}"
            echo "========================================"
            echo ""
            echo "📊 Application Status:"
            pm2 status
            echo ""
            echo "📝 Recent Logs:"
            pm2 logs ${{ env.APP_NAME }} --lines 15 --nostream
            echo ""
            echo "🔧 Useful Commands:"
            echo "  - View logs: pm2 logs ${{ env.APP_NAME }}"
            echo "  - Restart app: pm2 restart ${{ env.APP_NAME }}"
            echo "  - Stop app: pm2 stop ${{ env.APP_NAME }}"
            echo "  - App status: pm2 status"
            echo ""
            echo "========================================"

      # ==========================================
      # Step 9: Display Live URL (GitHub Actions Output)
      # ==========================================
      - name: 🎉 Display Live URL
        run: |
          echo ""
          echo "========================================="
          echo "🎉 DEPLOYMENT COMPLETE!"
          echo "========================================="
          echo ""
          echo "🌍 Your application is now live at:"
          echo ""
          echo "   🔗 http://${{ secrets.SSH_HOST }}"
          echo ""
          echo "========================================="
          echo ""
          echo "📝 Additional Information:"
          echo "  - Application: ${{ env.APP_NAME }}"
          echo "  - Port: ${{ env.APP_PORT }}"
          echo "  - Repository: ${{ env.NODE_PROJECT_URL_TO_DEPLOY }}"
          echo ""
          echo "✅ Access your website from anywhere on the internet!"
          echo "========================================="
